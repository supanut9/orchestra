/**
 * SkillManager — SKILL.md manager GUI (Lane C).
 */
export function SkillManager() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-[hsl(var(--muted-foreground))]">
      Skill Manager (SKILL.md) — coming soon
    </div>
  );
}
