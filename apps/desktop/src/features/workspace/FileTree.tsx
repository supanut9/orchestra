/**
 * FileTree — virtualized file explorer (Lane A).
 * Placeholder until Lane A implements notify-rs watcher + tree UI.
 */
export function FileTree() {
  return (
    <div className="flex h-full items-center justify-center p-4 text-xs text-[hsl(var(--muted-foreground))] text-center">
      File tree — open a workspace to begin
    </div>
  );
}
