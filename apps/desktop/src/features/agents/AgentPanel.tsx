/**
 * AgentPanel — AI agent chat sidebar (Lane C).
 * Placeholder until Lane C implements the model-agnostic agent runtime.
 */
export function AgentPanel() {
  return (
    <div className="flex h-full items-center justify-center p-4 text-sm text-[hsl(var(--muted-foreground))] text-center">
      Agent chat (model-agnostic) — coming soon
    </div>
  );
}
