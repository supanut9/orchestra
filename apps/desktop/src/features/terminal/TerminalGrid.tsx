/**
 * TerminalGrid — xterm.js multi-pane terminal grid (Lane B).
 * Placeholder until Lane B wires up portable-pty + xterm.
 */
export function TerminalGrid() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-[hsl(var(--muted-foreground))] bg-black/80 font-mono">
      Terminal grid (xterm.js + WebGL) — coming soon
    </div>
  );
}
