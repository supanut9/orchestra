/**
 * LaneBoard — parallel task lane manager (Lane C).
 * Placeholder until Lane C implements LangGraph coordinator + worktree UI.
 */
export function LaneBoard() {
  return (
    <div className="flex h-full items-center justify-center text-sm text-[hsl(var(--muted-foreground))]">
      Lane Board (parallel agent lanes) — coming soon
    </div>
  );
}
