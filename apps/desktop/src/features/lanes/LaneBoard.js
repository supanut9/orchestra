import { jsx as _jsx } from "react/jsx-runtime";
/**
 * LaneBoard — parallel task lane manager (Lane C).
 * Placeholder until Lane C implements LangGraph coordinator + worktree UI.
 */
export function LaneBoard() {
    return (_jsx("div", { className: "flex h-full items-center justify-center text-sm text-[hsl(var(--muted-foreground))]", children: "Lane Board (parallel agent lanes) \u2014 coming soon" }));
}
